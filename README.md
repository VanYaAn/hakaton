# Meeting Bot для MAX

Бот для организации встреч в мессенджере MAX с голосованием за удобное время.

## 🎯 Описание проекта

Meeting Bot помогает организовать встречи с коллегами через мессенджер MAX:
- Создание встреч с несколькими участниками
- Предложение временных слотов
- Голосование участников за удобное время
- Автоматическое определение лучшего времени
- Напоминания за 15 минут до встречи

## 🏗️ Архитектура

Проект использует Clean Architecture с разделением на слои:

```
.
├── cmd/
│   └── bot/           # Точка входа приложения
├── internal/
│   ├── domain/        # Бизнес-сущности (Meeting, User, Vote)
│   ├── repository/    # Слой доступа к данным
│   ├── service/       # Бизнес-логика
│   ├── handler/       # Обработчики команд бота
│   └── config/        # Конфигурация
└── pkg/               # MAX API (внешние библиотеки)
```

### Принципы:
- **Dependency Injection** - все зависимости внедряются через конструкторы
- **Interface-based design** - репозитории определены через интерфейсы
- **Stub implementations** - для быстрого прототипирования используются заглушки

## 🚀 Быстрый старт

### Предварительные требования

- Docker 20.10+
- Docker Compose 2.0+
- Go 1.21+ (для локальной разработки)

### Запуск с Docker Compose

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd meeting-bot
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Отредактируйте `.env` и укажите ваш MAX bot token:
```bash
BOT_TOKEN=your_max_bot_token_here
```

4. Запустите приложение:
```bash
docker-compose up --build
```

Бот будет доступен на `http://localhost:8080`

### Остановка приложения

```bash
docker-compose down
```

Для удаления данных БД:
```bash
docker-compose down -v
```

## 🛠️ Локальная разработка

### Сборка проекта

```bash
go build -o bot ./cmd/bot
```

### Запуск локально

```bash
# Установите переменные окружения
export BOT_TOKEN=your_token
export DATABASE_URL=postgres://localhost/meetingbot

# Запустите приложение
./bot
```

### Запуск тестов

```bash
go test ./...
```

## 🐳 Docker

### Сборка Docker-образа

```bash
docker build -t meeting-bot:latest .
```

### Запуск контейнера

```bash
docker run -p 8080:8080 \
  -e BOT_TOKEN=your_token \
  -e DATABASE_URL=postgres://db:5432/meetingbot \
  meeting-bot:latest
```

## 📋 Доступные команды бота

- `/start` - Приветственное сообщение
- `/help` - Список команд
- `/create_meeting "Название" @user1 @user2 14:00 15:00` - Создание встречи

### Пример использования

```
/create_meeting "Планирование спринта" @ivan @maria 14:00 15:00 16:00 17:00
```

Бот создаст встречу, предложит участникам проголосовать за время с помощью реакций (✅/👎),
и через 2 часа автоматически определит лучшее время.

## 🔧 Конфигурация

Настройки через переменные окружения:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BOT_TOKEN` | Токен MAX бота | `stub_token` |
| `DATABASE_URL` | URL подключения к PostgreSQL | `postgres://localhost/meetingbot` |
| `SERVER_PORT` | Порт сервера | `8080` |
| `MAX_API_BASE_URL` | Base URL MAX API | `https://api.max.ru` |
| `VOTING_DURATION` | Длительность голосования (минуты) | `120` |

## 📦 Зависимости

Основные зависимости указаны в `go.mod`:
- `github.com/lib/pq` - PostgreSQL драйвер

См. `requirements.txt` для полного списка требований.

## 🗄️ База данных

Проект использует PostgreSQL со следующими таблицами:

- `meetings` - информация о встречах
- `meeting_participants` - участники встреч
- `time_slots` - временные слоты для голосования
- `votes` - голоса участников

**Примечание:** В текущей версии используются in-memory заглушки.
Реальная интеграция с PostgreSQL будет добавлена на следующем этапе.

## 🔄 Workflow

1. Пользователь создает встречу командой `/create_meeting`
2. Бот создает встречу и отправляет уведомления участникам
3. Участники голосуют за удобное время реакциями ✅/👎
4. Через 2 часа бот подсчитывает голоса
5. Встреча подтверждается на время с максимальным количеством голосов
6. За 15 минут до встречи отправляется напоминание

## 🎨 Особенности реализации

### Dependency Injection
Все зависимости создаются в `main.go` и передаются через конструкторы:

```go
container := initContainer()
// Инициализация всех сервисов с правильными зависимостями
```

### Stub Repositories
Для быстрого прототипирования используются in-memory репозитории:
- `MeetingRepositoryStub`
- `VoteRepositoryStub`
- `UserRepositoryStub`

Они реализуют те же интерфейсы, что и будущие PostgreSQL репозитории.

### Логирование
Все stub-методы логируют свои вызовы с префиксом `[STUB]` для отладки.

## 🚧 Roadmap

- [ ] Интеграция с MAX API
- [ ] Реализация PostgreSQL репозиториев
- [ ] Парсинг команд с пользовательским вводом
- [ ] Обработка реакций для голосования
- [ ] Персистентная очередь для напоминаний
- [ ] Unit и интеграционные тесты
- [ ] Метрики и мониторинг

## 👥 Команда

Проект разработан командой для хакатона VK Education.

## 📄 Лицензия

MIT License
